export declare class RealmDirectoryService {
    private logger;
    private adminToken;
    private protectedRealmPaths;
    private discovery;
    private adminRealm;
    private setLogger(l);
    private findByPath(realmPath, req, ownerId?);
    private remove(realmPath, req);
    private start(server);
    private stop();
    private findSyncLabel();
    private findOrCreateRealmFile(realmPath, getOwner, syncLabel?);
}
